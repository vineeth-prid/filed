# Direct dependencies only. Transitive packages are resolved by pip.
# The fully-pinned, reproducible set is preserved in requirements.lock.txt
# (install that for byte-for-byte reproducible builds:
#   pip install -r requirements.lock.txt).

# --- Web framework / server ---
fastapi==0.110.1
uvicorn==0.25.0
starlette==0.37.2
python-multipart==0.0.30
pydantic==2.13.4

# --- Database ---
motor==3.3.1
pymongo==4.5.0

# --- Auth ---
bcrypt==4.1.3
PyJWT==2.13.0

# --- Config / HTTP ---
python-dotenv==1.2.2
httpx==0.28.1
requests==2.34.2

# --- HTML scraping ---
beautifulsoup4==4.15.0

# --- PDF extraction stack ---
pdfplumber==0.11.9
camelot-py==2.0.0
pytesseract==0.3.13
pdf2image==1.17.0
opencv-python-headless==4.13.0.92

# --- LLM (Emergent universal key wrapper). emergentintegrations depends on
#     the Emergent-hosted litellm wheel below; keep both pinned together. ---
emergentintegrations==0.2.0
litellm @ https://customer-assets.emergentagent.com/internal-asset/library/litellm-1.80.0-py3-none-any.whl#sha256=adf398c513273de9341f61822296c6b2145f7f2dc4a69daf3ac04829f5bde3f8

# --- Test / lint (dev) ---
pytest==9.0.3
